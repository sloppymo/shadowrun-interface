export interface Theme {
  name: string;
  background: string;
  text: string;
  secondaryText: string;
  accent: string;
  prompt: string;
  input: string;
  inputText: string;
  secondaryBackground: string;
} 